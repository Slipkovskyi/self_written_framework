RewriteEngine On

# Ensure that the requested URL is not a file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirect all requests to public/index.php
RewriteRule ^(.*)$ /public/index.php [L]
